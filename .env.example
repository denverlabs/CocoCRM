# Flask Configuration
SECRET_KEY=your-secret-key-here-change-this-in-production

# Telegram Bot Configuration
# To get a bot token:
# 1. Open Telegram and search for @BotFather
# 2. Send /newbot and follow instructions
# 3. Copy the bot token and paste it here
# 4. Copy your bot username (e.g., @YourBot_bot)
# 5. Set your bot domain with /setdomain command in BotFather
TELEGRAM_BOT_TOKEN=your-telegram-bot-token-here
TELEGRAM_BOT_USERNAME=@YourBot_bot

# Database (SQLite by default, can be changed to PostgreSQL, MySQL, etc.)
# SQLALCHEMY_DATABASE_URI=sqlite:///crm.db

# API Configuration (for AI Agent access)
# This key allows authorized AI agents (like Kimi) to generate temporary login tokens
TELEGRAM_API_KEY=your-secure-api-key-change-this
BASE_URL=https://your-domain.onrender.com

# OpenClaw Permanent API Key (Recommended for bot integration)
# This key NEVER expires - perfect for programmatic access
# If not set, will fallback to TELEGRAM_API_KEY
OPENCLAW_API_KEY=sk-openclaw-2026-your-permanent-secret-key-here

# Usage:
# - TELEGRAM_API_KEY: For temporary login tokens (3 hours)
# - OPENCLAW_API_KEY: For permanent API access (REST endpoints)
